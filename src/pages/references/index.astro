---
// Reference index. The card grid that used to sit on the homepage.
// observation list, competing with it for the same seven destinations.
import { getAllEntries, displayIndex } from '@/lib/entries';
import { ui, entryTranslations, categoryTranslations, allSectionHrefs } from '@/lib/i18n';
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';

const entries = (await getAllEntries()).filter((e) => e.data.category !== 'Perfil');
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const t = ui['pt'];
const cat = categoryTranslations['pt'] ?? {};
const home = base;
const hrefs = allSectionHrefs(base, 'references');
---
<Base title={`${t.home_refs} — Farol`} description={t.tagline} altHrefs={hrefs}>
  <Header lang="pt" altHrefs={hrefs} />
  <Breadcrumb lang="pt" crumbs={[{ label: t.breadcrumb_home, href: home }, { label: t.home_refs.toLowerCase() }]} />
  <div class="prompt-section">
    <h1 class="prompt-question">{t.home_refs}</h1>
  </div>
  <nav class="tree" aria-label={t.home_refs}>
    {entries.map((entry) => {
      const slug = entry.id.replace(/\.md$/, '');
      const tr = null as { title?: string; desc?: string } | null;
      return (
        <a href={`${home}${entry.collection}/${slug}/`} class="tree-node animate-in" data-category={entry.data.category}>
          <span class="tree-node-category">{cat[entry.data.category] ?? entry.data.category}</span>
          <span class="tree-node-index" aria-hidden="true">{displayIndex(entry.data)}</span>
          <span class="tree-node-title">{tr?.title ?? entry.data.title}</span>
          <span class="tree-node-desc">{tr?.desc ?? entry.data.desc}</span>
          <span class="tree-node-arrow">{t.consult} <span aria-hidden="true">→</span></span>
        </a>
      );
    })}
  </nav>
  <Footer lang="pt" />
</Base>
<style>
.tree-node[data-category="Referência"] { border-left: 3px solid #4a7c59; }
</style>
